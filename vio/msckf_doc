Sensors setup:
1. Xiaomi Stereo IMU hardware.

Datasets:
1. Static envioment data collection;
2. Indoor Parking zoom data collection;
3. Static envioment + Dynamic objects(control by us);

To do:
Sensors setup:
1. Order high accuracy Calibrate board;
2. Update the calibration parameters by new calibration board;
3. New Stereo + IMU hardware;
   eg.:  http://grauonline.de/wordpress/?page_id=1951  
   a. monochrome global shutter camera (mvBlueFox-MLC200wG, ON Semiconductor MT9V034 digital image sensor) connected via USB 
   (important: it has an external trigger pin we’ll use for hardware synchronization)
   b. 132 degree fisheye lense
   c. IMU: GY-88 (MPU6050) connected to Arduino Nano

Architecture:
1. Seperate diffent blocks, update the arch documents.
2. Support loop closure:   Deep loop closure.

Functions:
1. Dynamic Objects.
2. Time difference support.
3. Robust vio test;

4. Support localizition sparse map for loop dectect;
